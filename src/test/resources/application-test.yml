server.port: 8082

spring:
  application:
    name: billing
  datasource:
    url: jdbc:postgresql://localhost:5432/billing_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    user: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/billing_test
logging:
  level:
    org.flywaydb: DEBUG
    org.springframework.boot.autoconfigure.jdbc: DEBUG
    org.postgresql: DEBUG